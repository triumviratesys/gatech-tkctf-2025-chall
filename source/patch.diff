diff --git a/source/src/decimal_labyrinth_v2.c b/source/src/decimal_labyrinth_v2.c
index 1234567..abcdefg 100644
--- a/source/src/decimal_labyrinth_v2.c
+++ b/source/src/decimal_labyrinth_v2.c
@@ -119,10 +119,7 @@ void inscribe_data(void) {
     read_data(input, sizeof(input));
     size_t len = strlen(input);

-    if (corrupted == n) {
-        puts("[Overflow enabled - writing with extra bytes]");
-        memcpy(z->data, input, len > 0x88 ? 0x88 : len);
-    } else {
-        memcpy(z->data, input, len > 0x80 ? 0x80 : len);
-    }
+    // Fix: Always enforce proper bounds checking
+    memcpy(z->data, input, len > 0x80 ? 0x80 : len);

     printf("[Data inscribed to Zone %d]\n", n);
 }
